#include <stdio.h>

/**
 * swap_int - swaps the value of two integers
 * @a: 1st input integer
 * @b: 2nd input integer
 * Return: null
 */

void swap_int(int *a, int *b)
{
	int s = *a;
	*a = *b;
	*b = temp;
}
